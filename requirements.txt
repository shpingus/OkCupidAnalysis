# Core dependencies for Gemini Age Prediction System
# Install with: pip install -r requirements.txt

# Google Gemini API
google-generativeai>=0.3.0

# Data manipulation and analysis
pandas>=1.5.0
numpy>=1.21.0

# Machine Learning and Statistics
scikit-learn>=1.2.0
scipy>=1.9.0

# Core data processing and scientific computing
numpy>=1.20.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Deep learning
torch>=1.10.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# Progress bars and utilities
tqdm>=4.64.0
# JSON handling (usually included with Python)
# json - standard library

# Regular expressions (usually included with Python)
# re - standard library

# Date/time handling (usually included with Python)
# datetime - standard library
matplotlib>=3.4.0
seaborn>=0.11.0

# Utilities
tqdm>=4.60.0
# File operations (usually included with Python)
# os - standard library
# pickle - standard library

# Type hints (Python 3.8+)
# typing - standard library for Python 3.8+

# Optional: PyTorch for loading existing NN models (if you want to load actual trained models)
# torch>=1.13.0
# Comment out the torch line above if you don't need to load the actual NN models

# Optional: Jupyter notebook support
# jupyter>=1.0.0
# notebook>=6.4.0

# For environment variable management (alternative to config file)
python-dotenv>=0.19.0

# API access for embeddings
voyageai>=0.1.0
